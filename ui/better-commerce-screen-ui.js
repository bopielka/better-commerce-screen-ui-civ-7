/**
 * Better Commerce Screen UI by Najane - entry point.
 *
 * Nothing is patched yet; this only proves the mod is discovered and its scripts
 * run. `console.log` never reaches Logs\UI.log, so diagnostics use console.error.
 */

console.error('[better-commerce-screen-ui] loaded');

export {};
